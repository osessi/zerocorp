/**
 * Placeholder. The real route resolves the tenant from the host, loads the
 * published page version through a use case, and hands the validated block JSON
 * to @zerocorp/site-renderer.
 *
 * No business logic here — this file stays a thin adapter.
 */
export default function Page() {
  return <main>ZeroCorp tenant site renderer — scaffold</main>;
}
