/**
 * Placeholder. Real routes stay thin: parse, authenticate, authorize, invoke a
 * use case from @zerocorp/application, serialize the result.
 */
export default function Page() {
  return <main>ZeroCorp back-office — scaffold</main>;
}
