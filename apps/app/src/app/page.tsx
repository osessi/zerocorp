import { Landing } from "./Landing";

export const metadata = {
  title: "ZeroCorp — tell us where you are, we build the business system",
  description:
    "Describe what you are building. ZeroCorp works out what is missing, what to build first, and what it costs.",
};

export default function Page() {
  return <Landing />;
}
