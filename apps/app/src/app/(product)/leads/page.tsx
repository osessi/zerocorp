import { NotBuiltYet } from "../NotBuiltYet";

export const metadata = { title: "Customers — ZeroCorp" };

export default function Page() {
  return (
    <NotBuiltYet
      title="Customers"
      does="Define who you sell to, discover matching companies continuously, enrich the contact where it is lawful to, filter, save lists and export. V1 finds them; it does not contact them yet."
      needs="A lawful-basis model. A prospecting feature built without one is a liability rather than a feature, and that doctrine currently lives only in the archive (C2)."
    />
  );
}
