import { NotBuiltYet } from "../NotBuiltYet";

export const metadata = { title: "Brand — ZeroCorp" };

export default function Page() {
  return (
    <NotBuiltYet
      title="Brand"
      does="Name, positioning, ideal customer, value proposition, tone of voice, logo and colours, generated from your Business Brain and approved by you before anything is built on them."
      needs="The deep onboarding that fills the Business Brain. Everything generated here traces back to it, so it comes first."
    />
  );
}
