import { NotBuiltYet } from "../NotBuiltYet";

export const metadata = { title: "Content — ZeroCorp" };

export default function Page() {
  return (
    <NotBuiltYet
      title="Content"
      does="Keyword strategy, an editorial calendar, articles written, reviewed, approved and published on a schedule you set. Up to five a day, and that is a publication ceiling rather than a generation limit."
      needs="The brand and the website. Publishing before either exists produces articles with nowhere to live and no voice to write in."
    />
  );
}
