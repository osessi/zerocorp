import { NotBuiltYet } from "../NotBuiltYet";

export const metadata = { title: "Help — ZeroCorp" };

export default function Page() {
  return (
    <NotBuiltYet
      title="Help"
      does="How to reach a human, what ZeroCorp is doing right now, and the answers to the questions founders actually ask at each stage."
      needs="Enough of the product to have questions about."
    />
  );
}
