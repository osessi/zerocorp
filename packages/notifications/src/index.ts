/**
 * @zerocorp/notifications — Layer 3
 *
 * One dispatch layer for in-app, email and Telegram. Notifications derive from
 * recorded domain events and activity, so product logic is never duplicated
 * inside a template.
 */
export const NOTIFICATIONS_PACKAGE = "@zerocorp/notifications" as const;
