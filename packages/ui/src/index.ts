/**
 * @zerocorp/ui — Layer 4
 *
 * React components for the authenticated product: apps/app back-office and admin
 * console. Built strictly on @zerocorp/design-system tokens.
 *
 * Shared by every authenticated surface so components are never duplicated.
 * This is not the tenant website system — that is @zerocorp/site-renderer.
 */
export const UI_PACKAGE = "@zerocorp/ui" as const;
