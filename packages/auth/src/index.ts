/**
 * @zerocorp/auth — Layer 3
 *
 * Sessions, identity and authorization policies. Implements the application
 * layer's authorization ports. Admin surfaces require MFA.
 */
export const AUTH_PACKAGE = "@zerocorp/auth" as const;
