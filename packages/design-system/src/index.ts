/**
 * @zerocorp/design-system — Layer 0
 *
 * Tokens and primitives only: color, typography, spacing, radius, elevation,
 * motion, breakpoints. No product knowledge, no business components.
 *
 * Consumed by @zerocorp/ui (back-office) and @zerocorp/site-renderer (tenant
 * sites), which are two different visual systems built on the same token layer.
 *
 * Token values are NOT yet decided — see docs/OPEN_DECISIONS.md D-G6.
 */
export const DESIGN_SYSTEM_PACKAGE = "@zerocorp/design-system" as const;
